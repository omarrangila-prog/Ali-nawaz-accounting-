node_modules
dist
dist-ssr
*.local
.env
.env.local
.DS_Store
.firebase
firebase-debug.log
*.log
.vscode/*
!.vscode/extensions.json

# TS build info
*.tsbuildinfo

# Electron build output
release

.vercel

# Migration backups: live financial records, kept on disk as the undo path
# for the tools/ scripts. Not pushed — say the word if you want them in git.
tools/*-backup.json
